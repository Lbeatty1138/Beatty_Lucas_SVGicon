(() => {
	// elf-invoking anonymous fuctions (or lambda)
	// var => means creat a variable
	// what ever cmes after the = is what the variable
	var	images = document.querySelectorAll('img');

	// document is always the webpage
	// after the dot is always a method or a property

	console.log(images);
})();